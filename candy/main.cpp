#include "bits/stdc++.h"

int main(){
   std::vector<int> arr = {100, 1, 3, 0, 10, 100};
   int P = 3;

   // std::sort(a.begin(), a.end(), [](const std::vector<int> &x, const std::vector<int> &y)
   //            { return x[1] < y[1]; });

   std::set<int> set = {1, 3, 4 };
   int res = 0;
   std::vector<int> max(arr.size(), 0);
   int max = 0;
   for(int i = arr.size() - 1; i >= 0; --i){

   }


}